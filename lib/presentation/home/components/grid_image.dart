import 'package:flutter/material.dart';

class GridIImage extends StatelessWidget {
  const GridIImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
